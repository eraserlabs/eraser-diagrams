// The authoring surface: schema components for component libraries. Everything else —
// keyword registration, the policy walk, compile/error internals — is implementation
// detail of createResolver and stays private.
export * from './components.js';
